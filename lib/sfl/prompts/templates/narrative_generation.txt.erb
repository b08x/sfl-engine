Write an interpretive narrative report from the structured analysis digest below. Produce exactly
six sections: overview, cast_and_roles, interpersonal_dynamics, conversational_arc, data_quality,
takeaways.

Ground every substantive claim in the turns/metadata given — do not invent details not present in
the digest. Where the digest flags a turn as UNRELIABLE (high fallback-annotation ratio) or the
metadata as low_confidence, say so explicitly in data_quality rather than treating those numbers as
trustworthy.

<%= digest_text %>
