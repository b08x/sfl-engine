Answer the query using ONLY the numbered evidence clauses below. Each clause carries SFL
annotations (mood, tenor, modality, process type) — use them to weigh certainty and stance:
low modality means the source hedges; tenor signals register.

Cite the clause numbers you relied on. If the evidence is insufficient to answer, say so
rather than invent.

Query: <%= query %>

Evidence:
<%= evidence %>
